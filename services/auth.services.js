// import models from '../models';

// export const createUser = async(userInfo) => {
//     let err, user;
//     [ err ] = await to (models.sequelize.transaction(async(t) => {
//         [ err, user ] = await to (models.)
//     }))
// }